---
description:
globs:
alwaysApply: false
---
# Project Structure Overview

This is a Next.js project with the following key directories:

-   `src/app/`: Contains the main application code, including pages, API routes, and layouts, following the Next.js App Router conventions.
    -   `src/app/api/`: Specifically for API route handlers.
-   `src/components/`: Houses reusable UI components.
    -   `src/components/ui/`: Likely contains primitive UI components, possibly from a library like Shadcn/UI (given `components.json`).
-   `src/hooks/`: For custom React hooks.
-   `public/`: Stores static assets like images, fonts, etc., accessible directly via the base URL.
-   `prisma/`: Contains Prisma schema (`schema.prisma`) and potentially migrations for database management.

Key configuration files include:
-   `next.config.ts`: [next.config.ts](mdc:next.config.ts) - Configuration for the Next.js framework.
-   `tsconfig.json`: [tsconfig.json](mdc:tsconfig.json) - TypeScript compiler options.
-   `package.json`: [package.json](mdc:package.json) - Project dependencies and scripts.
-   `components.json`: [components.json](mdc:components.json) - Likely configuration for UI components (e.g., Shadcn/UI).